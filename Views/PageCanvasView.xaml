<UserControl
    x:Class="Scriptum.Views.PageCanvasView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border Padding="24">
        <Grid RowSpacing="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Spacing="4">
                    <TextBlock Text="Notebook Capture"
                               FontSize="28"
                               FontWeight="SemiBold" />
                    <TextBlock Text="{x:Bind ViewModel.ImageDetails, Mode=OneWay}"
                               Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                </StackPanel>

                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            Spacing="8"
                            VerticalAlignment="Center">
                    <Button Content="Import Image" Click="ImportImageButton_Click" />
                    <Button Content="Load Latest" Click="LoadLatestButton_Click" />
                    <Button Content="Save" Click="SaveButton_Click" />
                    <Button Content="Prepare Transcription" Click="PrepareTranscriptionButton_Click" />
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Background="White"
                        BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}"
                        BorderThickness="1"
                        CornerRadius="8">
                    <Grid MinHeight="620">
                        <ScrollViewer HorizontalScrollBarVisibility="Auto"
                                      VerticalScrollBarVisibility="Auto"
                                      ZoomMode="Enabled">
                            <Image Source="{x:Bind ViewModel.PageImage, Mode=OneWay}"
                                   Stretch="Uniform"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center" />
                        </ScrollViewer>

                        <StackPanel x:Name="EmptyState"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Spacing="8">
                            <TextBlock Text="No notebook page imported"
                                       FontSize="20"
                                       FontWeight="SemiBold"
                                       TextAlignment="Center" />
                            <TextBlock Text="Use Import Image to select a photo or scan from your physical notebook."
                                       TextAlignment="Center"
                                       Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Grid.Column="1"
                        BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16">
                    <Grid RowSpacing="16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <StackPanel Spacing="6">
                            <TextBlock Text="{x:Bind ViewModel.PageTitle, Mode=OneWay}"
                                       FontSize="20"
                                       FontWeight="SemiBold" />
                            <TextBlock Text="{x:Bind ViewModel.SourceFileLabel, Mode=OneWay}"
                                       TextWrapping="Wrap"
                                       Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                        </StackPanel>

                        <Border Grid.Row="1"
                                Background="{ThemeResource SystemControlBackgroundBaseLowBrush}"
                                Padding="12"
                                CornerRadius="8">
                            <TextBlock Text="{x:Bind ViewModel.ImageDetails, Mode=OneWay}"
                                       TextWrapping="Wrap" />
                        </Border>

                        <StackPanel Grid.Row="2" Spacing="8">
                            <TextBlock Text="Transcription"
                                       FontSize="18"
                                       FontWeight="SemiBold" />
                            <TextBox Text="{x:Bind ViewModel.TranscriptionText, Mode=OneWay}"
                                     AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     IsReadOnly="True"
                                     MinHeight="280" />
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Row="2" ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
                           Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
            </Grid>
        </Grid>
    </Border>
</UserControl>
